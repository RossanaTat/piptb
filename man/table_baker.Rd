% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_baker.R
\name{table_baker}
\alias{table_baker}
\title{Table baker to interact with API}
\usage{
table_baker(
  vars = NULL,
  weight = NULL,
  col = NULL,
  row = NULL,
  scol = NULL,
  srow = NULL,
  by_vars = c(col, row, scol, srow),
  stats = "mean",
  format = c("long", "wide"),
  povline = 1.9,
  by_survey = getOption("by_survey.tb"),
  id_var = c("cache_id", "survey_id"),
  max_country = getOption("piptb.max_country"),
  max_survey = getOption("piptb.max_survey"),
  arrow_format = c("parquet", "feather"),
  root_dir = Sys.getenv("PIP_DATA_ROOT_FOLDER"),
  data_connect = NULL,
  ...
)
}
\arguments{
\item{vars}{variables to analyze. If "pov_status" selected, estimates will be
done for poverty status for each value in `povlines` If NULL, size
population of population in each group is calculated}

\item{weight}{sampling weight variable}

\item{col}{column variable}

\item{row}{row variables}

\item{scol}{super column variable}

\item{srow}{super row variable}

\item{by_vars}{combination of `c(col, row, scol, srow)`}

\item{stats}{Statistics to estimate}

\item{format}{character: Either "long" or "wide". Default is "long"}

\item{by_survey}{logical: If TRUE include variable "cache_id" as part of the
grouping variables.}

\item{id_var}{characger: variable used to uniquely identify surveys. It could
be "cache_id" or "survey_id". Default is "cache_id".}

\item{data_connect}{object of class "FileSystemDataset",  "Dataset",
"ArrowObject", and  "R6". It holds the connection to the pre-computed
indicators of the table baker}

\item{...}{arguments with survey years for each country. Argument name should
be three-letter country code in upper cases. For instance, it should be of
the form `COL = c(2010, 2012)` to get household survey data for Colombia
(COL) for 2010 and 2012.}
}
\value{

}
\description{
Table baker to interact with API
}
\examples{
\dontrun{
dw <- table_baker(COL = c(2010, 2012),
  HND = c(2006:2010),
  PRY = c(2006,2010),
  BRA = 2018)
}
}
