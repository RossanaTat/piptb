% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_dta2R.R
\name{tm_dta2R}
\alias{tm_dta2R}
\title{Convert dta GMD files to R binary files}
\usage{
tm_dta2R(
  country = NA,
  year = NULL,
  survey = NA,
  vermast = NA,
  veralt = NA,
  module = "PX",
  maindir = ":/03.ProjectX/data/",
  drive = "p"
)
}
\arguments{
\item{country}{character: list of country iso3 code (accepts multiple) or
`all`.}

\item{year}{numeric:  list of years.}

\item{survey}{charecter: In case there are more than one surveys for the same year}

\item{vermast}{numeric: Master version. Default is the latest version}

\item{veralt}{numeric: Alternative version. Default is the latest version}

\item{maindir}{character: Main directory where data is stored}

\item{drive}{character: Drive letter when data is stored. Default is P}

\item{type}{character: Collection. Default is GPWG}
}
\value{
A dataframe with two columns, one for the id of the survey and one for the status
of the saving procedure.
}
\description{
Pending: add option to check if the file already exists, so it is skipped
Pending: add conditions if user select name of survey
pending: check that paramters `year` or `survey` can only be specified if
parameter `country` is also specified.
}
\examples{
e <- tm_dta2R(country = "PRY", year = c(2013, 2014))
}
