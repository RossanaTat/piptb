# piptb 0.1.0

* Add option of `vars = NULL` for the calculation of population size in each group.

# piptb 0.0.1

* Added a `NEWS.md` file to track changes to the package.
* First working version of `tb()` not tested yet. 
