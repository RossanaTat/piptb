#' Check inputs from tb function
#'
#' @inheritParams tb
check_input_tb <- function(.data,
                            vars,
                            weight,
                            col,
                            row,
                            scol,
                            srow,
                            by_vars,
                            stats) {

  # check by vars are all nse or se
  return(invisible(TRUE))

}
